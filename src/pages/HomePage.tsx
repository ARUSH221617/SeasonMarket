import React from "react";
import { Gemini } from "../contexts/Gemini";

const HomePage = (props) => {
  return (
    <div className="flex-1 min-h-full">
    </div>
  );
};
export default HomePage;
